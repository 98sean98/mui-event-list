import { createMuiTheme, Theme } from "@material-ui/core";

export const defaultTheme: Theme = createMuiTheme({
  palette: {
    primary: {
      main: "#4285F4"
    },
    secondary: {
      main: "#4caf50"
    }
  }
});
