import { createMuiTheme } from "@material-ui/core";

export const defaultTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#4285F4"
    },
    secondary: {
      main: "#4caf50"
    }
  }
});
